(Contribution||has||Model)
(Model||has||neural network model called RNN Encoder - Decoder)
(neural network model called RNN Encoder - Decoder||consists of||two recurrent neural networks ( RNN ))
(neural network model called RNN Encoder - Decoder||has||One RNN that encodes a sequence of symbols into a fixed-length vector representation)
(neural network model called RNN Encoder - Decoder||has||the other RNN that decodes the representation into another sequence of symbols)
(neural network model called RNN Encoder - Decoder||is jointly trained to||maximize the conditional probability of a target sequence given a source sequence)
(Approach||has||neural network architecture called RNN Encoder - Decoder)
(neural network architecture called RNN Encoder - Decoder||consists of||two recurrent neural networks ( RNN ) that act as an encoder and a decoder pair)
(neural network architecture called RNN Encoder - Decoder||maps||a variable-length source sequence to a fixed-length vector)
(neural network architecture called RNN Encoder - Decoder||maps||the vector representation back to a variable-length target sequence)
(neural network architecture called RNN Encoder - Decoder||is trained jointly to||maximize the conditional probability of the target sequence given a source sequence)
(Approach||proposes||a rather sophisticated hidden unit to improve both the memory capacity and the ease of training)
(Model||has||neural network architecture)
(neural network architecture||called||RNN Encoder - Decoder)
(RNN Encoder - Decoder||is able to||learn the mapping from a sequence of an arbitrary length to another sequence , possibly from a different set , of an arbitrary length)
(RNN Encoder - Decoder||is able to||score a pair of sequences ( in terms of a conditional probability ))
(RNN Encoder - Decoder||is able to||generate a target sequence given a source sequence)
(Model||has||novel hidden unit)
(novel hidden unit||includes||a reset gate and an update gate)
(novel hidden unit||adaptively control||how much each hidden unit remembers or forgets while reading / generating a sequence)
